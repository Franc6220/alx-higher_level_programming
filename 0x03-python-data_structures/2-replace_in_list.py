#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    [[row[my_list] for row in matrix] for my_list in range(4)]
    listlength = len(my_list) - 1    #Since my_list is from 0 to 5 this enables it to be equals to listlength after this 
    if (idx < 0 and idx > listlength)
        return (my_list)
    elif (idx > 0 and idx < listlength)
        return (new_list)
    else:
         mylist[idx] = element
         return (my_list)
